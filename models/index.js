const User = require('./user');
const Game = require('./game');
const Tournament = require('./tournament');
const HistoricalGame = require('./historicalGame');

module.exports = {
    User,
    Game,
    Tournament,
    HistoricalGame
}; 